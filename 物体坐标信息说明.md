# RoboTwin 仓库物体坐标信息说明

## 概述
RoboTwin 是一个双臂机器人操作平台，使用 SAPIEN 物理引擎。在这个仓库中，物体的坐标信息可以很方便地获取。

## 核心类和方法

### 1. Actor 类
位置：`/workspace/envs/utils/actor_utils.py`

`Actor` 类是对 SAPIEN 实体的包装，提供了便利的方法来访问物体信息。

#### 主要方法：
- **`get_pose()`** - 获取物体的完整位姿
  - 返回：`sapien.Pose` 对象
  - 包含位置（`p`）和旋转四元数（`q`）

- **`get_point(type, idx, ret)`** - 获取特定点的位姿
  - 参数 `type`："contact", "target", "functional", "orientation"
  - 参数 `ret`：返回格式 "matrix", "list", "pose"

### 2. sapien.Pose 对象
从 `get_pose()` 返回的对象具有：
- **`p` 属性**：位置坐标，NumPy 数组 [x, y, z]
- **`q` 属性**：旋转四元数，NumPy 数组 [w, x, y, z]

## 使用示例

### 基本使用
```python
# 获取物体位姿
pose = actor.get_pose()

# 获取位置坐标
position = pose.p  # [x, y, z]
x, y, z = position[0], position[1], position[2]

# 获取四元数
quaternion = pose.q  # [w, x, y, z]
```

### 在任务环境中使用
以 `place_a2b_left` 任务为例：
```python
from envs.place_a2b_left import place_a2b_left

# 创建环境
env = place_a2b_left()
env.setup_demo(task_name="place_a2b_left")

# 获取物体
object_a = env.object
object_b = env.target_object

# 获取物体 A 的位置
pose_a = object_a.get_pose()
position_a = pose_a.p
print(f"物体 A 的位置: {position_a}")
```

## 代码库中的相关文件

| 文件路径 | 说明 |
|---------|------|
| `/workspace/envs/utils/actor_utils.py` | 定义了 Actor 和 ArticulationActor 类 |
| `/workspace/envs/_base_task.py` | 基础任务类，包含场景设置和物体加载方法 |
| `/workspace/envs/place_a2b_left.py` | 具体任务实现，展示了物体创建和使用 |
| `/workspace/envs/utils/create_actor.py` | 物体创建函数 |

## 额外信息

除了基本的位置信息，Actor 类还提供了：
- **接触点（contact points）**：物体上可以抓取的点
- **目标点（target points）**：用于任务的目标位置
- **功能点（functional points）**：物体上的功能位置（如抽屉把手等）

这些点可以通过 `get_contact_point()`, `get_target_point()`, `get_functional_point()` 等方法获取。
